import RegisterForm from "@/components/auth/register-form"

export const metadata = {
  title: "Create Account — EduTask",
  description: "Create your EduTask account",
}

export default function RegisterPage() {
  return <RegisterForm />
}
